// Lag en funksjon 'everyother' med signaturen under.
// Funksjonen skal ta med annenhvert element av en liste, 
// dvs element 1, 3, 5 osv.
// Et par eksempler:
// everyother [1 .. 10] -> [1;3;5;7;9]
// everyother ['a' .. 'e'] -> ['a';'b';'c';'d';'e']
// everyother ["hello"; "world"] -> ["hello"]
// everyother [] -> []

// Lag en funksjon 'everyotter' som er nesten lik som 'everyother',
// men som tar med element 2, 4, 6 osv.

let everyother (lst : 'a list) : 'a list = []

let everyotter (lst : 'a list) : 'a list = []
